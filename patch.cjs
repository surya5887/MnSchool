const fs = require('fs'); let c = fs.readFileSync('src/components/LivePaperBuilder.tsx', 'utf8'); c = c.replace(/useState<\{ type: 'section' \| 'question', sIdx: number, qIdx\?: number \} \| null>\(null\);/g, useState<{ type: 'section' | 'question' | 'endText', sIdx?: number, qIdx?: number } | null>(null);); fs.writeFileSync('src/components/LivePaperBuilder.tsx', c); console.log('Done 6!');
